services:
  nukiblinker:
    build: .
    container_name: nukiblinker
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./homekit:/app/.homekit
