services:
  nukiblinker:
    image: ghcr.io/ndobler/nukiblinker:latest
    container_name: nukiblinker
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    # Logs to stdout by default; uncomment to persist:
    # - ./logs:/app/logs
